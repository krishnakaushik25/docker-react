sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t krishnakaushik11/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true krishnakaushik11/docker-react npm run test
